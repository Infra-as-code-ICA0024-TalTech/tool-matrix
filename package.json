{
  "name": "ica0024-tool-matrix",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "serve": "python3 -m http.server 8080 --directory site"
  },
  "engines": {"node": ">=22"}
}
